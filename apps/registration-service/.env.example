# Registration Service
NODE_ENV=development
PORT=3003

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=practicas_user
DB_PASSWORD=practicas_password
DB_NAME=practicas_registration
DB_LOGGING=false
DB_SSL=false

# Security
JWT_SECRET=dev-shared-jwt-secret
JWT_ISSUER=registration-service

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:4200,http://localhost:4000
