name: Deploy Gateway

on:
  push:
    branches: [main, qa]
    paths:
      - 'apps/api-gateway/**'
      - '.github/workflows/gateway-deploy.yml'

env:
  AWS_REGION: us-east-1
  ASG_NAME: practicas-gateway-asg

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: dapaeza
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build, tag, and push api-gateway image
        env:
          IMAGE_TAG: ${{ github.sha }}
        run: |
          REPO=dapaeza/practicas-api-gateway
          docker build -t $REPO:latest -t $REPO:$IMAGE_TAG -f apps/api-gateway/Dockerfile .
          docker push $REPO:latest
          docker push $REPO:$IMAGE_TAG

      - name: Start ASG Instance Refresh
        run: |
          aws autoscaling start-instance-refresh \
            --auto-scaling-group-name $ASG_NAME \
            --preferences '{"MinHealthyPercentage": 50, "InstanceWarmup": 300}'
